"""The cloning methods: how an experiment joins its fragments into one plasmid.

One method is one package here, and `plan` is what every method's plan shares. Golden Gate is
the method this package holds today. Nothing is re-exported: import a method by module path,
as `liulab_mbio.cloning.goldengate`.
"""
