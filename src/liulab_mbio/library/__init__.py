"""Assembling a barcoded library in rounds: what each round covers, and what it takes.

`coverage` counts the distinct constructs a design yields and the colonies each round needs for
the coverage asked for. `bench` turns the method's own volumes and masses into amounts at the
design's real lengths. Import either by module path.
"""
